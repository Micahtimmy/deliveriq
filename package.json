{
  "name": "ici-dashboard",
  "version": "1.0.0",
  "main": "src/resolvers/index.ts",
  "license": "MIT",
  "scripts": {
    "test": "vitest run --pool=forks",
    "typecheck": "tsc --noEmit",
    "build": "npm run build --prefix static/ici-app"
  },
  "devDependencies": {
    "@types/node": "^18.15.0",
    "typescript": "^5.0.4",
    "vitest": "^1.3.1"
  },
  "dependencies": {
    "@forge/api": "^4.2.0",
    "@forge/resolver": "^1.5.7"
  }
}

